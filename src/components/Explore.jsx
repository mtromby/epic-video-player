import { Box, Typography } from '@mui/material';

const Explore = () => {
  return (
    <Box sx={{ p: 2 }}>
      <Typography variant="h4" component="h1" gutterBottom>
        Explore
      </Typography>
      <Typography variant="body1">
        Discover new content and experiences.
      </Typography>
    </Box>
  );
};

export default Explore; 